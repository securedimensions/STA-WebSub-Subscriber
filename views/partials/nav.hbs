<nav class="navbar navbar-expand-lg navbar-light bg-white py-3 shadow-sm">
    <div class="container" id="headerContainer">
        <a class="navbar-brand" href="/">
            <img alt="" class="d-inline-block align-top" height="30" src="/images/logo.png" width="30">
            <font color="#008000" face="Britannic Bold">
                <b>{{agentName}}</b>
            </font>
        </a>
        <button aria-controls="navbars" aria-expanded="false" aria-label="Toggle navigation" class="navbar-toggler"
            data-bs-target="#navbarContent" data-bs-toggle="collapse" type="button">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav mr-auto">
                {{#each customNavLinks}}
                <li class="nav-item">
                    <a class="nav-item nav-link {{this.class}}" href="{{this.url}}">{{this.label}}</a>
                </li>
                {{/each}}
            </ul>
            <ul class="navbar-nav ml-auto">
                {{#if user}}
                {{#each navLinks}}
                    <a class="nav-item nav-link {{this.class}}" href="{{this.url}}">{{this.label}}</a>
                {{/each}}
                <li class="dropdown">
                    <a id="dropdownUser" href="#" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
                        class="nav-link dropdown-toggle"><i class="fa fa-user"></i> {{user.Username}}</a>
                    <ul aria-labelledby="dropdownUser" class="dropdown-menu border-0" role="menu">
                        <li class="dropdown-divider"></li>
                        <li><a href="/user/logout" id="logout-btn" class="dropdown-item"><i
                                    class="fa fa-sign-out"></i>Logout</a></li>
                    </ul>
                </li>
                {{else}}
                <li class="nav-item"><a href="/user" class="nav-link"><i class="fa fa-sign-in"></i>Sign In</a></li>
                {{/if}}
            </ul>
        </div>
        <div id="statusLight" class="spinner-grow text-light" role="status">
            <span class="sr-only">Loading...</span>
        </div>
    </div>
</nav>

<script>
    window.addEventListener('load', function () {

    });

</script>